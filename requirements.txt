# --- runtime core ---
aiohttp
anyio
python-dotenv
requests
pydub

# --- observability ---
prometheus-client
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-grpc

# --- web api ---
fastapi
uvicorn[standard]

# --- data utils ---
pandas
tqdm
scipy
librosa>=0.8.1
soundfile
matplotlib

# --- VoIP/Asterisk ARI ---
asyncari

# --- BigVGAN dependencies (excluding ML stack) ---
# BigVGAN được vendor trong thư mục /vendor, đây là các dependencies của nó
tensorboard
pesq
auraloss
nnAudio
ninja

# --- STT ---
google-cloud-speech>=2.26.0
grpcio>=1.62.0

# --- Orchestrator/Agent ---
langchain
langgraph

# --- Pydantic v2 cho FastAPI ---
pydantic>=2.7

# --- YAML parser ---
pyyaml>=6.0